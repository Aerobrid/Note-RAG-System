# ── Web Framework ─────────────────────────────
fastapi>=0.115.5,<0.135.2
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── Config & Types ────────────────────────────
pydantic>=2.11.0,<2.12.5
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ── LangChain / LangGraph ─────────────────────
langchain>=0.3.7,<0.4.0
langchain-core>=0.3.20,<0.4.0
langchain-community>=0.3.7,<0.4.0
langchain-google-genai>=2.0.7,<3.0.0
langchain-ollama>=0.2.1,<0.3.0
langgraph>=0.2.53,<0.3.0

# ── Document Parsing (IBM Docling) ────────────
docling>=2.70.0,<2.82.0

# ── Vector Store ──────────────────────────────
chromadb==0.5.23

# ── Embeddings & Reranking ────────────────────
sentence-transformers==3.3.1
FlagEmbedding==1.3.5

# ── BM25 for Hybrid Retrieval ─────────────────
rank-bm25==0.2.2

# ── File Watching (auto-indexing) ─────────────
watchdog==6.0.0

# ── Async Utilities ───────────────────────────
httpx==0.28.1
aiofiles==24.1.0
anyio==4.7.0

# ── Fine-tuning (install separately on GPU) ───
# Run: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# Then: pip install trl transformers datasets accelerate bitsandbytes
